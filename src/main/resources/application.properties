spring.application.name=TCSWPractica05

# PostgreSQL database configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/Empleados
spring.datasource.username=postgres
spring.datasource.password=kimi
spring.datasource.driver-class-name=org.postgresql.Driver

# Logging configuration (typo corrected)
logging.level.org.springframework.web=DEBUG

# JPA/Hibernate configuration (hidden characters removed)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect